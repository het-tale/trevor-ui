import { Canvas, Meta } from "@storybook/blocks";
import * as SearchListStories from "./SearchList.stories";

<Meta of={SearchListStories} />

# SearchList

This the official documentation for the `SearchList` component.
